abbr -a gcl git clone
abbr -a gcg --set-cursor "git clone github:%"
abbr -a gco git checkout
abbr -a gfap git fetch --all --prune
abbr -a gp git pull
abbr -a gpl git pull
abbr -a gpr git pull --rebase
abbr -a gu git push
abbr -a gs git status
abbr -a ls eza
abbr -a cat "bat -p"
abbr -a open --set-cursor "open % 2>/dev/null"
abbr -a sus --set-cursor "sort % | uniq -c | sort -nr"
abbr -a pipi pip install
abbr -a pipu pip install --upgrade
abbr -a replace-in-last-command --regex "^\^[^\^]+\^[^\^]+" --function __mmb_replace_in_last_cmd
